#run these for various batch renames
#todo: make this interactive

#for adding prefix
for file in *.<file_extension> ; do mv "${file}" "<prefix>_${file}"; done 

#for adding suffix
for file in *.<file extension> ; do mv "${file}" "${file%.<extension>}_suffix.<extension>"; done
